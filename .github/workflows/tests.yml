name: Tests

on: push

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 2

      - name: Install Node.js and NPM
        uses: actions/setup-node@v2
        with:
          node-version: 20

      - name: Get npm cache directory
        id: npm-cache-dir
        run: echo "dir=$(npm config get cache)" >> ${GITHUB_OUTPUT}

      - uses: actions/cache@v4
        id: npm-cache # use this to check for `cache-hit` ==> if: steps.npm-cache.outputs.cache-hit != 'true'
        with:
          path: ${{ steps.npm-cache-dir.outputs.dir }}
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: node --version && npm install

      # allow exit code 0 or 4 because of a bug that returns 4 even if the tests pass
      - name: Run tests and allow exit code 0 or 4
        run: npm test; code=$?; if [ "$code" -eq 0 ] || [ "$code" -eq 4 ]; then exit 0; else exit $code; fi
